# Supabase Configuration
# Get these from your Supabase project dashboard

# Server-side configuration (Netlify Functions)
# Use the service_role key for server-side operations (full permissions)
SUPABASE_URL=https://qoinlxpumoakfmkfrwqb.supabase.co
SUPABASE_SERVICE_KEY=sb_secret_VOTFopyG5pqE_RgYbUkYuQ_sfUDFdum

# Alternative: Next.js-style environment variables (also supported)
# Use these if you're migrating from Next.js or prefer this convention
NEXT_PUBLIC_SUPABASE_URL=https://qoinlxpumoakfmkfrwqb.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=sb_publishable_WLf3k1wUdS8oCm8Jwame8A_foZ2djIF

# Direct Database Access (Optional)
# For running migrations, SQL scripts, or using database management tools
# Get the database password from your Supabase project settings
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.qoinlxpumoakfmkfrwqb.supabase.co:5432/postgres

# Key Types Explained:
# - Service Key (sb_secret_*): For server-side operations, full database permissions
# - Publishable Key (sb_publishable_*): For client-side, restricted by RLS policies
# - Database URL: Direct PostgreSQL connection for migrations and admin tools
# 
# For Netlify Functions, use SUPABASE_SERVICE_KEY (server-side)
# The system will fallback to NEXT_PUBLIC_* variables if standard ones aren't set

# Admin Authentication
# Generate hash with: node scripts/hash-password.js "your-password"
ADMIN_PASSWORD_HASH=your-bcrypt-hash-here

# Optional: Email Double Opt-in
# Set to 1 to keep emails in pending state until confirmed
ENABLE_DOUBLE_OPT_IN=0

# Optional: TikTok Pixel
# Can also be set directly in HTML
TIKTOK_PIXEL_ID=

# Optional: Admin Allowed Origins (comma-separated)
ADMIN_ALLOWED_ORIGINS=https://dropchargeadmin.netlify.app,https://dropcharge.netlify.app,http://localhost:8888
